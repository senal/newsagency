name : NewsAgency - Paper service Docker CI/CD pipeline

on:
  push:
    branches:
      - main
    paths:
      - papers/**

jobs:
  build:
    runs-on: ubuntu-latest
    env:
      WORKING_DIR: ./papers
    steps:
      - name: Build the docker image 🚢 
        uses: actions/checkout@v2
        run: docker builld -t ranga/paperservice:$(date +%s) .